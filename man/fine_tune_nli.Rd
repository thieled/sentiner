% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/06_targeted_nli_tuning.R
\name{fine_tune_nli}
\alias{fine_tune_nli}
\title{Fine-tune an NLI model via Python}
\usage{
fine_tune_nli(
  data,
  model_name = "MoritzLaurer/deberta-v3-base-zeroshot-v2.0",
  run_name = NULL,
  output_dir = NULL,
  doc_id_col = "doc_id",
  text_col = "text",
  target_col = "target",
  label_col = "label",
  hypothesis_template = "The sentiment towards {target} is {label}.",
  train_prop = 0.7,
  downsample_train = TRUE,
  learning_rate = 2e-05,
  train_batch_size = 16L,
  eval_batch_size = 80L,
  num_epochs = 3L,
  warmup_ratio = 0.25,
  weight_decay = 0.1,
  seed = 42L,
  use_fp16 = FALSE,
  entailment_index = NULL,
  contradiction_index = NULL,
  ...
)
}
\arguments{
\item{data}{Annotated data frame (one row per document–target pair).}

\item{model_name}{HuggingFace model name or local path.}

\item{run_name}{Short name for this training run (used in the output
directory and training logs).  Auto-generated from \code{model_name}
if \code{NULL}.}

\item{output_dir}{Directory for the fine-tuned model.
Auto-generated as \code{models/<run_name>} if \code{NULL}.}

\item{doc_id_col, text_col, target_col, label_col}{Column names in \code{data}.}

\item{hypothesis_template}{\code{\link[glue]{glue}} template with
\code{{target}} and \code{{label}} placeholders.}

\item{train_prop}{Training proportion.}

\item{downsample_train}{Down-sample false training examples.}

\item{learning_rate, train_batch_size, eval_batch_size, num_epochs, warmup_ratio, weight_decay}{Training hyper-parameters.}

\item{seed}{Random seed.}

\item{use_fp16}{Use mixed-precision training (FP16; requires CUDA).}

\item{entailment_index, contradiction_index}{NLI output indices.
\code{NULL} (default) triggers auto-detection from the model config.}

\item{...}{Additional arguments to pass to the \code{initialize_sentiner} function.}
}
\value{
Path to the saved model directory (character).
}
\description{
Prepares NLI data with \code{\link{prepare_nli_data}}, then calls the Python
training function via \pkg{reticulate}.
}
