% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/00_installs.R
\name{install_conda_env}
\alias{install_conda_env}
\title{Install and Set Up a Conda Environment}
\usage{
install_conda_env(
  conda_env_name = "r-sentiner",
  python_version = "3.11",
  conda_path = NULL,
  check_path = TRUE,
  ask = TRUE,
  force = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{conda_env_name}{Character. The name of the Conda environment to create or use.
Defaults to \code{"r-sentiner"}.}

\item{python_version}{Character. Python version to use when creating the Conda environment.
Defaults to \code{"3.11"}. If the environment already exists, the version is not changed
unless \code{force = TRUE}.}

\item{conda_path}{Character. Optional path to a Miniconda installation. If \code{NULL},
autodetects or installs Miniconda to the default location.}

\item{check_path}{Logical. Checks whether the installed conda is on PATH. Default is TRUE.}

\item{ask}{Logical. If \code{TRUE}, prompts the user before installing Miniconda.
Default is \code{TRUE}.}

\item{force}{Logical. If \code{TRUE}, removes an existing Conda environment with the same
name before recreating it with the requested Python version. Default is \code{FALSE}.}

\item{verbose}{Logical. If \code{TRUE}, prints progress messages. Default is \code{TRUE}.}
}
\value{
Invisibly returns \code{NULL}. Called for its side effects of installing and
configuring the Conda environment.
}
\description{
Ensures Miniconda is installed, creates (or recreates) a Conda environment
with the specified name and Python version, and activates it.
This function does not handle fastText installation, as the R package
\pkg{fastText} should be used instead.
}
