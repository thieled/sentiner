% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/03_fuzzy_matching.R
\name{split_labels}
\alias{split_labels}
\title{Split and Clean Label Strings}
\usage{
split_labels(
  dt,
  id_col = "id",
  label_col = "label",
  group_col = NULL,
  pattern = "\\\\|",
  keep_orig = TRUE,
  keep_first = FALSE,
  disambiguate = TRUE
)
}
\arguments{
\item{dt}{A data.frame or data.table with at least an ID and label column.}

\item{id_col}{Character. Name of the ID column. Default is "id".}

\item{label_col}{Character. Name of the label column. Default is "label".}

\item{group_col}{Character or NULL. Optional group column name. Default is NULL.}

\item{pattern}{Character regex pattern to split labels. Default is "\\|".}

\item{keep_orig}{Logical. Keep the original labels as rows. Default is TRUE.}

\item{keep_first}{Logical. Keep only the first split label per row. Default is FALSE.}

\item{disambiguate}{Logical. Remove labels that appear in multiple IDs
(globally or within groups). Default is TRUE.}
}
\value{
A data.table with columns \code{id} (user-specified name), \code{label},
and optionally the user-specified group column.
}
\description{
Splits a label column into long format, applies trimming, removes duplicates,
and optionally disambiguates labels globally or within groups.
}
